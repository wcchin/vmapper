<g class="{{ layername }}">
    {% if showlabel: %}
    <title>{{ label }}</title>
    {% endif %}
    <line id="{{ idd }}" x1="{{ x0 }}" y1="{{ y0 }}" x2="{{ x1 }}" y2="{{ y1 }}" class="line" {{ style_str }} > {{ anim_str }} </line>
</g>
