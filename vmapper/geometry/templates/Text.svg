<g class="{{ layername }}">
    <text x="{{ x }}" y="{{ y }}" text-anchor="{{ text_anchor }}" dominant-baseline="central" class="area" {{ style_str }} > {{ anim_str }}
        {{ text }}
    </text>
</g>
